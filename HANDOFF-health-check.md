# HANDOFF: 建立 HireFlow 每日資料健檢腳本（daily_health_check.py）
> 更新：2026-08-06 12:45｜上手模型建議：sonnet｜理由：純新增一支獨立唯讀腳本，邏輯明確、不動既有寫入路徑
> 位置：C:\Users\User\Desktop\HR_AI_Assistant｜相關日誌：H:\我的雲端硬碟\Obsidian筆記用\創作庫\Vibe Coding專案\日誌\2026-08-05 HireFlow職缺同步遺漏bug修復.md
> 目前主機：使用者主機（Windows 11）｜目前 harness：Claude Code｜換手原因：主 session context 已長，此任務可完全獨立完成
>
> ⚠️ **本檔不是 `HANDOFF.md`**。專案根目錄另有一份 2026-07-13 的 `HANDOFF.md`（招募→留任回饋迴路），
> 它第 1 節仍有 2 項未完成（已於 2026-08-06 查證：06_員工主檔的 4 個留任欄位確實存在，但 5 個欄位
> 全部 0 筆有值，代表寫入路徑從沒被真正驗證過）。兩份互不相干，**不要合併、不要刪除那一份**。

## 1. 目標與驗收條件

原始需求（使用者原話，不改寫）：
> 那你寫一個handoff 我叫另一個session做

任務本體來自 2026-08-06 Opus 5 架構洞察的「長期方向 1（最高優先）」：
> 每天自動跑一次資料健檢，用你看得懂的中文報結果。理由不是「應該要有測試」這種通則，
> 而是這一年的維護史全是同一個模式：**資料悄悄壞掉，過了幾週才因為某個數字看起來怪而被發現**。
> 應徵批次日期被蓋成今天、面試通過率永遠 0%、01_職缺主檔從沒被寫過、欄位錯位從第 9 欄開始全錯、
> 報給主管的 154 其實該是 102。共同點不是「bug 難修」，是**發現得太晚**。

驗收條件（全勾才算完成）：
- [ ] `python daily_health_check.py` 可在專案根目錄執行，全程**唯讀**（不寫 Sheets、不寫任何本機資料檔）
- [ ] 輸出是使用者（非工程師）看得懂的繁體中文，每個問題都指出「哪一列／哪個職缺／哪個人」
- [ ] 第 5 節列出的 9 項檢查全部實作，且**每一項都對當下真實資料實跑驗證過**
- [ ] 反向驗證：在測試副本裡故意製造一筆重複 application_id，確認第 3 項真的會紅（只有正向通過不算驗證）
- [ ] `--email` 參數可在「有問題時」寄報告給自己；**沒問題時不寄**（每天都寄會被忽略，等於沒做）
- [ ] `python -m pytest tests/test_smoke.py -q` 仍然全綠（目前 17 條）
- [ ] 交付一行可直接貼進 Windows 工作排程器的指令，附白話設定步驟

## 2. 現況

已完成並驗證（2026-08-06 主 session，皆已 commit + push）：
- **application_id 撞號修復**（commit `18de0d2`）：新增 `resolve_candidate_code()` / `make_master_ids()`。836 筆裡 825 筆 ID 完全不變、撞號 5→0；Sheets 上 11 列已原地改名（非新增列），侯政宇的「錄取審核」進度完整保住；實跑推進+復原驗證過
- **兩道寫入護欄**：`dashboard._row_still_matches()`（寫入前確認那列還是那個人）、`dashboard._safe_headers()`（表頭重複/空白欄名時拒絕寫入）。正反案例都實測過
- **01_職缺主檔同步遺漏修復**（commit `c9ab0ce`）：新增 `_build_job_row()`
- smoke test 12 → 17 條，全綠

未開始：
- **本任務：`daily_health_check.py`**（尚未建立任何檔案）

## 3. 已排除的路徑（禁止重試）

- **不要用 `履歷原文[:300]` 做 hash 或指紋比對** → 104 履歷前 300 字是「履歷使用規範」法律樣板、人人完全相同，所有人會 hash 到同一個值。要用全文。（`app.py:891` docstring 有記）
- **不要用 regex 從 `履歷原文` 反抓「代碼: 12345」當那個人的 104代碼** → 實測 9 筆裡 2 筆抓到同一個代碼、2 筆抓到垃圾值「2」，因為那些原文是 104 多人份列印預覽合併檔，抓到的是「檔案裡第一個出現的代碼」
- **不要在腳本裡刪除或插入 Sheets 的列** → 刪列會讓後面列號位移，而 dashboard.py 用 60 秒 TTL 快取的列號寫入，位移會寫到別人身上（2026-08-05 翁志魁被誤寫成「錄取審核」就是這樣造成的）。本腳本一律唯讀
- **不要用 `next((r for r in rows if r.get('姓名')==name))` 這類非唯一欄位找資料** → 一人可能有多筆應徵紀錄，會抓到錯的那筆。要比對就用 `application_id`
- **不要把檢查塞進 `tests/test_smoke.py`** → 那 17 條守的是「離線程式契約」（不連 Sheets）；健檢守的是「線上資料自洽性」。兩者刻意分開（Opus 判斷）
- **不要為了讓健檢全綠而去修資料** → 報出來的問題，修不修、怎麼修都是使用者的決定

## 4. 關鍵檔案

- `hr_schema.py` — 欄名/狀態值單一來源。會用到 `S1_COLS`、`S3_COLS`、`SHEET_HEADERS`、`FLOW_TO_STAGE`
- `app.py:822` `_get_gsheet_client(spreadsheet_id)` — gspread 連線（服務帳號模擬），直接重用
- `app.py:891` `resolve_candidate_code(cand)` — 候選人唯一碼的正本公式
- `app.py:922` `make_master_ids(cand, jd_name)` — 回傳 (cand_id, app_id, scr_id, job_safe)
- `app.py:544` `_load_library_payload(jd_name)` / `app.py:559` `load_resume_library(jd_name)` — 讀本機履歷庫
- `app.py:1415` `send_recommendation_email()` — 現成 Gmail SMTP 範例；`email_config.json` 有 `sender_email`/`app_password`/`smtp_server`/`smtp_port`
- `scripts/migrate_colliding_app_ids.py` — **最佳參考樣板**：獨立腳本連 Sheets、有 `--dry-run`、繁中輸出、逐列比對

## 5. 下一步（從這裡開始）

**第一個動作**：在專案根目錄建立 `daily_health_check.py`。連線那段直接抄 `scripts/migrate_colliding_app_ids.py` 的 `main()` 前 10 行（讀 `gsheet_config.json` → `google_auth_default` → `impersonated_credentials` → `gspread.authorize`）。**先只實作下面第 1 項檢查、跑起來看到輸出**，再往下加——不要 9 項全寫完才第一次執行。

**九項檢查**（每項都對應一個真實踩過的坑，括號是事故日期）：

| # | 檢查 | 判紅條件 | 為什麼（真實事故） |
|---|---|---|---|
| 1 | 03 表頭健康 | 第 1 列有重複欄名、空白欄名，或欄數 ≠ `len(S3_COLS)` | 表頭塌掉會讓 dashboard 靜默寫到錯的欄位，且是全表系統性錯 |
| 2 | 每列欄數 | 任一列欄數 ≠ schema 欄數 | 07-09：13 欄 vs 19 欄錯位，第 9 欄之後全錯 |
| 3 | application_id 唯一 | 有任何重複值 | 08-05：8 列共用 3 個 ID，4 位真實 A/B 級候選人在看板上被合併成 1 人 |
| 4 | 職缺對得上 | 03 的 `job_id` 在 01_職缺主檔找不到 | 08-04：01 從沒被自動同步過，新職缺在看板完全看不到 |
| 5 | 無佔位字串 | `職缺名稱`/`job_id` 含 `➕ 新增自訂職缺` 或開頭 `__` | 07-14（葉宇騫）：佔位字串寫進主檔，之後所有比對都找不到那筆 |
| 6 | 流程狀態合法 | 值不在 `FLOW_TO_STAGE` 的 key 裡 | 打錯字會讓看板讀成 screening，那個人從看板消失 |
| 7 | 面試紀錄不缺 | `流程狀態` 是「錄取審核」或「已通知」，但 05_面試主檔用 application_id 查不到 | 07-23：面試通過率永遠 0%。也是「狀態跟前一階段接不上」（翁志魁那類）的偵測器 |
| 8 | 應徵日期沒被覆寫 | 同一個 `應徵批次日期` 的筆數 > 100，或不重複日期值 < 8 種 | 07-27：831 筆日期被同步流程蓋成只剩 4 種值 |
| 9 | 履歷庫 vs Sheets 筆數 | 任一職缺兩邊筆數差 > 2 | 同步漏跑會靜默少人。**已知合理落差**：「王美嵐」1 筆孤兒紀錄 |

**輸出格式要求**：全過就印一行 `✅ 今日健檢通過（檢查 9 項 / 838 筆應徵紀錄）`。有問題則每項一段：

```
⚠️ [3/9] application_id 重複 —— 2 組
   APP--三創晚班計時員：第 234、236 列（姓名：張三／(空白)）
   → 意義：這些人在招募看板上會被合併成一個人，按推進會改到錯的人
   → 建議：跑 scripts/migrate_colliding_app_ids.py --dry-run 看修法
```

每個問題都要有「意義」（非工程師看得懂的後果）與「建議」（下一步動作），不要只丟技術訊息。

**之後依序**：
1. 對真實資料實跑，逐項確認輸出正確。**預期第 5 項會抓到已知的「王美嵐」孤兒紀錄**（`APP-30000003058837-__新增自訂職缺`）——抓到才證明這項有效
2. 反向驗證：複製一份測試資料、故意放一筆重複 application_id，確認第 3 項真的會紅
3. 加 `--email`（只在有問題時寄，重用 `email_config.json` 的 SMTP 設定）
4. 交付 Windows 工作排程器設定：一行指令 + 白話步驟（建議每天早上 8:30）
5. 收尾：更新 Obsidian 專案日誌、auto-memory `project_hireflow_ats.md`，然後**刪掉本檔**（HANDOFF 是 L3 拋棄式紀錄）

## 6. 邊界（不要動的東西）

- **一律唯讀**：不寫 Google Sheets、不寫 `resume_library/`、不寫 `audit_log.json`。職責只有「發現並報告」；修復另開腳本、且要讓使用者確認後才跑
- **不改 `app.py` / `dashboard.py` / `hr_schema.py`**：本任務純新增。若健檢過程發現這三個檔案有 bug，**寫進報告回報使用者，不要順手改**——它們是生產程式，剛在 `18de0d2` 大改過
- **不動 `tests/test_smoke.py` 既有 17 條**：可新增，不可改動或刪除既有斷言
- **不要覆蓋或刪除專案根目錄的 `HANDOFF.md`**（那是另一個未完成任務，見本檔開頭警示）
- **API 配額**：Sheets 一次 `get_all_values()` 抓整張表後在記憶體處理，不要逐列/逐格讀（會撞 429，這系統已因配額出現過紅字錯誤）
- 遇到「某筆資料是對是錯沒有客觀依據」的情況，**列進報告請使用者判斷，不要自己猜或自動修**（2026-08-03 已定原則：資料清理只處理有其他佐證的重複，沒佐證的留著不動）
