# HANDOFF: 招募系統資料資產化 — 招募→留任回饋迴路 + 後續建置方向

> 更新：2026-07-13 16:15｜上手模型建議：sonnet（後續都是整合既有函式/照既有模式加欄位，非架構判斷）
> 位置：C:\Users\User\Desktop\HR_AI_Assistant\｜相關日誌：Obsidian「創作庫/Vibe Coding專案/日誌/2026-07-13 HireFlow認證重構與職缺身份系統性修復.md」
> 目前主機：使用者本機 Windows｜目前 harness：Claude Code｜換手原因：任務段落完成，使用者要求先留一份完整交接紀錄

## 1. 目標與驗收條件

原始需求（使用者原話，不改寫）：
> 我想要都可以留存... 因為我們只是150-200人小公司，很多需求並沒有真的那麼需要，但總覺得所有的人事相關流程都應該要更系統化、模組化
> e先做吧
> 好了 加好了 幫我把需要做的事情寫成一個hand off 包含未來完整的建置方向

背景一句話：這不是合規急迫性任務，是「把招募資料留存變成可以優化的資本」——核心是把 AI 初篩評分是否準確、哪個評分維度真的預測留任，串回實際到職結果，未來才能回頭驗證/調整評分邏輯。

驗收條件（全勾才算完成）：
- [x] Google Sheets「06_員工主檔」已手動新增4個欄位：三個月考核結果／試用期通過／離職日／離職原因類別（使用者已確認完成）
- [ ] 實際在 Onboarding 頁面對一位到職者填一筆留任追蹤資料、存檔後去 Google Sheets 上肉眼核對有沒有真的寫進正確欄位（目前只過 py_compile + streamlit 啟動測試，沒有人真的操作過一次）
- [ ] 同上，順便驗證這次順手修的「薪資（月）」欄位也真的有寫入（之前是填了會顯示已儲存但實際沒存進去）

## 2. 現況

已完成並驗證：
- `hr_schema.py`「06_員工主檔」schema 補上 4 個留任欄位 + 補回原本漏列的「薪資（月）」，跟使用者提供的真實 Sheet 欄位順序完全比對過，一致
- `save_hire()` / `fetch_all_hires()` 加上這 4 個欄位的讀寫映射
- `page_onboarding()` 新增「🎯 留任 / 考核追蹤」收合區塊 UI
- 同時修掉一個既有 bug：Offer 表單填的「月薪」過去從未寫進 Sheet（`fetch_all_hires` 原本寫死 `"proposed_salary": None`，`save_hire` 的 row_data 完全沒有薪資欄位映射）——現已修正並接上「薪資（月）」欄位
- 以上全部過 `python -m py_compile` + 實際 `streamlit run` 啟動測試（app.py 與 dashboard.py 都測過，無 traceback）

已完成未驗證：
- 上面驗收條件裡打勾未打勾的兩項——需要使用者本人在真實環境操作一次並肉眼核對 Google Sheets

未開始（Fable 判斷過的後續建置方向，見第7節完整版）：
- B：104配對信半自動匯入（解析器 `parse_104_email.py` 已完成且用真實信件樣本測過，21個欄位全部正確；缺的只是「信怎麼進來、怎麼接進 app.py 現有初篩流程」這一層）
- A：JD協作 prompt（不是系統，是一份文字模板）
- D：新人 chatbot——拆兩半，提醒功能（GAS 定時任務）可早做；問答功能要等內部知識庫文件化才能做
- E 的分析端：季度手動跑的「AI評分 vs 留任結果」分析，樣本要等到職滿3個月以上才有意義，現在才剛開始留存資料，最快也要一季後才有第一批可分析樣本

## 3. 已排除的路徑（禁止重試）

- 104代碼/Email ↔ 員工編號**不要做自動比對**（fuzzy match by Email/姓名）→ 風險是同名、換Email、內推沒經過104的人會產生錯配，錯配進回饋迴路比沒資料更糟 → 已用「到職登錄時人工選一次」取代（`page_onboarding` 既有的 `save_hire()` 寫入 `candidate_id`/`job_id` 就是在做這件事，不用另建比對機制）
- B **不要做全自動信箱掃描+自動觸發初篩**（無人值守常駐管線）→ 104改一次信件版型就會靜默壞掉，非工程師維護風險太高，招募是低頻高風險場景不值得 → 改半自動：Gmail 設篩選規則把配對信集中到一個 label，app.py 加「從信件匯入」一鍵按鈕
- A **不要做成審批系統/網頁表單**→ 一年開缺頻率只有20-40次，蓋系統的維護成本永遠回不了本 → 改成一份「JD問診prompt」，HR收到用人申請時人工帶主管跑一次
- D 的**問答式 chatbot 現在不要做**→ 內部規章/流程/FAQ 還沒結構化成知識庫，沒有知識庫的 chatbot 只會產生幻覺答案，對 HR 場景是負價值 → 等知識庫文件化到位（這是 HR 工作，不是工程工作）再議
- D **不要跟 A/B/C/E 整合在同一套系統裡**→ 使用者不同（自己 vs 全體同仁）、技術棧必然不同（Streamlit本機需人操作 vs GAS雲端常駐）、資料重疊極薄（只共用「報到日」一欄）→ D 獨立立案

## 4. 關鍵檔案

- `hr_schema.py:61-70` — 06_員工主檔欄位定義，含 candidate_id/job_id 這個招募↔留任的橋接鑰匙，以及這次新增的4個留任欄位
- `dashboard.py:774-806` — `save_hire()`，所有寫進06_員工主檔的動作都經過這裡（新增欄位要在這裡加映射）
- `dashboard.py:591-624` — `fetch_all_hires()`，讀取06_員工主檔給 UI 用
- `dashboard.py:1796-1919` — `page_onboarding()`，到職checklist + Offer詳情 + 這次新增的留任追蹤UI都在這個函式裡
- `parse_104_email.py` — B 要用的104配對信解析器，已完成且測過，缺的是「信怎麼進來」這一層
- `app.py` 約2620行「📥 Stage 2: 匯入履歷」附近 — B 未來要接的入口點

## 5. 下一步（從這裡開始）

第一個動作：打開 dashboard.py 的 Onboarding 頁面，找一位已經在06_員工主檔裡的到職者，展開「🎯 留任 / 考核追蹤」，填一筆測試用的「三個月考核結果」存檔，然後直接去 Google Sheets 網頁版肉眼核對那一列有沒有真的寫進正確欄位（不能只看 Streamlit 顯示「✅已儲存」就當作驗證過）。同時可以順便測「薪資（月）」那個修復。

之後依序：
1. 確認上面驗收沒問題後，這個任務就算完整結束，之後就是「每次有人到職就記得填」的日常習慣，不需要再改程式
2. 等第一批到職滿3個月的樣本出現（因為系統今天才開始留存，最快約2026年10月才有第一筆有意義的考核資料），暫時不用急著做分析工具
3. 有空時做 B：在 app.py Stage 2 加一個「從 Gmail label 匯入」按鈕，串起已經寫好的 `parse_104_email.py`
4. 之後做 A：寫一份 JD 問診 prompt（不是程式任務，是內容任務）

## 6. 邊界（不要動的東西）

- 不要建 104代碼/Email 跟員工編號的自動比對機制，永遠靠到職登錄時人工選一次
- 不要把 D（新人chatbot）跟這套招募系統的程式碼整合在一起，技術棧本來就該分開（D 走 GAS+Google Chat，這套系統留在 Streamlit 本機）
- E 的分析端（AI評分準確度分析）先不要做，樣本不夠只會產生誤導性結論——建議至少累積 30 個以上滿3個月的到職案例再開始分析
- 目前 `app.py`/`dashboard.py`/`hr_schema.py` 有未 commit 的修改（`git status` 顯示3個檔案 modified），使用者尚未要求 commit，接手者不要自行決定 commit 時機，除非使用者明講

## 7. 未來完整建置藍圖（Fable判斷，本次任務前已確認，供長期參考）

這是這次任務誕生的完整脈絡，Fable 針對「招募到新人3個月訓練整合」給的一次性架構判斷，五塊分別是：

| 項目 | 內容 | 判斷 | 狀態 |
|---|---|---|---|
| **C** | 現有AI初篩→人工複審→推薦流程 | 不動，唯一已驗證的資產 | 運作中 |
| **E** | 招募→留任回饋迴路（candidate_id串06主檔+留任欄位） | 最優先，工程量小但有時效性 | **本次任務已完成留存端，分析端待樣本累積** |
| **B** | 104配對信自動化匯入 | 縮成半自動（一鍵匯入，非全自動常駐） | 解析器已完成，缺匯入入口 |
| **A** | 用人主管JD協作agent | 縮成一份prompt，不做系統 | 未開始 |
| **D** | 新人chatbot（適應+提醒） | 獨立立案，拆兩半（提醒可早做/問答待知識庫） | 未開始 |

建置順序：E → B → A，D 完全獨立於前四者之外，且 D 內部先做「提醒」不做「問答」。

技術骨幹原則：「人在操作時才需要跑」的東西（A的prompt、B的半自動匯入、C、E）留在現有 Streamlit本機+Google Sheets骨幹；「沒有人在場也要活著」的東西（D的常駐chatbot）才需要 GAS雲端常駐——但無論哪種技術棧，Google Sheets 六主檔永遠是唯一資料真相來源，不接受兩份資料真相並存。

完整原始 Fable 判斷（含每一項的詳細理由）在本次對話紀錄中，尚未落成 Obsidian 知識庫筆記——如果這份 HANDOFF 執行到一個段落後判斷這個架構決策值得長期保存，記得用 /寫入大腦 蒸餾成一篇知識庫筆記，而不是只留在這份會被刪除的拋棄式交接文件裡。
